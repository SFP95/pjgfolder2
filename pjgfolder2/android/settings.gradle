// settings.gradle

include ':app'

// Si estás usando Flutter, es necesario agregar estas líneas:
pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()  // Añadido para que Flutter y otros plugins puedan ser reconocidos
    }
}
